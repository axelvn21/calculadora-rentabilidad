services:
  - type: web
    name: calculadora-rentabilidad
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && python web_main.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
